<Window x:Class="CRM.DesktopClient.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:CRM.DesktopClient.ViewModels"
         d:DataContext="{d:DesignInstance viewModels:MainViewModel}"
        xmlns:v="clr-namespace:CRM.DesktopClient.Views"
        

        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        >
    <Window.Resources>
        <DataTemplate DataType="{x:Type viewModels:CustomersMainViewModel}">
            <v:CustomersMainView/>
        </DataTemplate>
    </Window.Resources>
    <StackPanel>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Button Command ="{Binding ChangeCurrentViewCommand}" Content="Customers" FontSize="30" Height="70" ></Button>
            <Button Command ="{Binding ChangeCurrentViewCommand}" Content="Timetable" FontSize="30" Height="70" Grid.Column="1"></Button>
        </Grid>
        <ContentControl Content="{Binding CurrentPageViewModel}" />
    </StackPanel>
</Window>
